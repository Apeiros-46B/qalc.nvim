TODO before merging rewrite->main
- write cmake package for libqalculate, that way we don't have to rely on pkg-config
- add nix package? might as well go all in. right now we just have a build script that works on nix but isn't really "pure" (it's still designed for use with a traditional neovim config, just on a system with nix available)
